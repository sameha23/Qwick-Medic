import Third from "@/components/Appointment/Third";

export const metadata = {
  title: {
    default: "Appointment",
  },
};

const page = () => {
  return (
    <div>
     
    <Third/>

    </div>
  );
};

export default page;